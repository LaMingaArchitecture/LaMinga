---
/**
 * Right chevron — outlined path (currentColor), the site-wide "suivant" control.
 * Replaces the delivered Fleche D asset, which shipped as a live Helvetica "›" <text> glyph and so
 * broke wherever Helvetica was unavailable; redrawn here as a font-independent stroked path.
 */
interface Props {
  size?: number | string;
  class?: string;
}
const { size = 24, class: className } = Astro.props;
---

<svg
  class:list={['icon', className]}
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 24 24"
  width={size}
  height={size}
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
  aria-hidden="true"
  focusable="false"
>
  <path d="m9 5 7 7-7 7"></path>
</svg>

<style>
  .icon {
    display: block;
  }
</style>
