---
import BaseLayout from '../layouts/BaseLayout.astro';
import HomePage from '../storyblok/HomePage.astro';
import { getHomePage, getProjectSummaries } from '../lib/content';

const home = await getHomePage();
const projects = await getProjectSummaries();
---

<BaseLayout title="LaMinga — Atelier d'architecture">
  <HomePage blok={home} projects={projects} />
</BaseLayout>
